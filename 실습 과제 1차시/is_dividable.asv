%  약수인지 판단
function output = is_dividable (int1, int2)
% 입력: int1: 피제수(dividend), int2: 제수(divisor)
% 출력: 약수이면 1, 그렇지 않으면 0
% int1에서 int2값을 한번씩 빼면서 
% 남은값이 0이하가 되도록 반복 (while 이용)
% 남은값이 정확히 0이면 약수
temp = 
while temp>=0
    temp = int1-int2;
if int1-int2
    output = 1; % 나머지가 0이면 1 출력
else
    output = 0; % 나머지가 0이 아니면 0 출력
end
end